name: CI
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - uses: step-security/harden-runner@03bee3930647ebbf994244c21ddbc0d4933aab4f # v2.3.0
        with:
          egress-policy: audit
          disable-sudo: true
      - uses: actions/checkout@v3
      - name: Invoke workflow without inputs
        uses: buf-setup-action@2bd65419f0264d12e114649664d7f86966fd9c90
        with:
          workflow: CI 2
